# conda env create -f environment.yml
# conda activate rga

name: pyrga
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy>=1.21
  - scipy>=1.8
  - matplotlib>=3.5
  - pyamg>=4.2
  - sympy>=1.10
  - pip:
    - mat73
    - joblib
