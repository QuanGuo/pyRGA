# conda env create -f environment.yml
# conda activate rga

name: rga
channels:
  - conda-forge
dependencies:
  - python=3.10
  - numpy>=1.21
  - scipy>=1.8
  - matplotlib>=3.5
  - pyamg>=4.2
  - sympy>=1.10
  - pip
  - pip:
      - mat73
      - joblib
